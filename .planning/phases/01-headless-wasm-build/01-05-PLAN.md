---
phase: 01-headless-wasm-build
plan: 05
type: execute
wave: 2
depends_on: ["01-04"]
files_modified:
  - scripts/test-wasm-load.js
  - scripts/test-threading.js
  - scripts/test-memory.js
  - scripts/test-main-loop.js
autonomous: false
gap_closure: true
requirements: [BUILD-01, BUILD-02, BUILD-03, BUILD-04, BUILD-05, BUILD-06, BUILD-07, BUILD-08]

must_haves:
  truths:
    - "WASM binary loads in Node.js without errors and wasm_init() returns 0"
    - "A .blend file loaded via wasm_load_blend() can be queried with wasm_query_scene() returning valid JSON"
    - "SharedArrayBuffer and Web Workers are operational (pthreads functional)"
    - "Memory allocation stays within 4GB ceiling and wasm_memory_usage() reports accurate bytes"
    - "Dev server serves WASM with correct COOP/COEP headers in a browser"
  artifacts:
    - path: "build-wasm/blender.wasm"
      provides: "Validated WASM binary (runtime-proven)"
    - path: "build-wasm/blender.wasm.br"
      provides: "Validated compressed binary (size-proven)"
  key_links:
    - from: "scripts/test-wasm-load.js"
      to: "build-wasm/blender.js"
      via: "require() loading Emscripten glue code"
      pattern: "require.*blender"
    - from: "web/index.html"
      to: "build-wasm/blender.js"
      via: "script tag loading"
      pattern: "blender\\.js"
    - from: "web/serve.py"
      to: "web/index.html"
      via: "HTTP serving with COOP/COEP headers"
      pattern: "Cross-Origin"
---

<objective>
Validate the compiled WASM binary through runtime tests, proving all Phase 1 requirements are satisfied at execution time.

Purpose: Plan 04 produced the WASM binary. This plan proves it actually WORKS by running the Node.js test scripts (test-wasm-load.js, test-threading.js, test-memory.js, test-main-loop.js), loading a .blend file, and verifying the dev server delivers the binary with correct headers in a browser. This converts the 6 "PARTIAL" requirement statuses from VERIFICATION.md to "SATISFIED".

Output: All 4 test scripts pass, .blend file loading confirmed, browser test page functional
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-headless-wasm-build/01-VERIFICATION.md
@.planning/phases/01-headless-wasm-build/01-02-SUMMARY.md
@.planning/phases/01-headless-wasm-build/01-04-SUMMARY.md

Key test/validation files:
@scripts/test-wasm-load.js
@scripts/test-threading.js
@scripts/test-memory.js
@scripts/test-main-loop.js
@web/serve.py
@web/index.html

<interfaces>
<!-- From Plan 04 output -->
build-wasm/blender.wasm -- compiled WASM binary
build-wasm/blender.js -- Emscripten JS glue code
build-wasm/blender.wasm.br -- Brotli compressed binary

<!-- From Plan 02 test scripts -->
scripts/test-wasm-load.js: Loads blender.js, calls wasm_init via ccall, PASS/FAIL
scripts/test-threading.js: Checks SharedArrayBuffer, Atomics, PThread workers
scripts/test-memory.js: Checks HEAP8.buffer.byteLength, wasm_memory_usage, 4GB ceiling
scripts/test-main-loop.js: 10s timeout, event loop responsiveness, PROXY_TO_PTHREAD

<!-- From Plan 02 dev server -->
web/serve.py: Python HTTP server with COOP/COEP headers
web/index.html: Browser test page checking COI, SharedArrayBuffer, SIMD, wasm_init
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run Node.js validation tests and fix any runtime failures</name>
  <files>scripts/test-wasm-load.js, scripts/test-threading.js, scripts/test-memory.js, scripts/test-main-loop.js</files>
  <read_first>
    - scripts/test-wasm-load.js (WASM binary loading and initialization test)
    - scripts/test-threading.js (SharedArrayBuffer and pthreads test)
    - scripts/test-memory.js (memory allocation and 4GB ceiling test)
    - scripts/test-main-loop.js (non-blocking main loop test)
  </read_first>
  <action>
    Run all four Node.js WASM validation scripts against the compiled binary. These scripts were created in Plan 02 and are designed to validate BUILD-01 through BUILD-08.

    **Execution order (sequential -- each depends on binary loading):**

    1. Run: `node scripts/test-wasm-load.js`
       - Validates: BUILD-01 (WASM loads), BUILD-02 (dependencies linked)
       - Expected: Loads build-wasm/blender.js, calls wasm_init() via ccall, prints "PASS:" and exits 0

    2. Run: `node scripts/test-threading.js`
       - Validates: BUILD-03 (pthreads/SharedArrayBuffer/Web Workers)
       - Expected: SharedArrayBuffer available, Atomics work, PThread pool has workers, exits 0

    3. Run: `node scripts/test-memory.js`
       - Validates: BUILD-04 (memory within 4GB, MEM_guarded_alloc)
       - Expected: HEAP8 buffer exists, wasm_memory_usage() returns bytes, ceiling check passes, exits 0

    4. Run: `node scripts/test-main-loop.js`
       - Validates: BUILD-06 (non-blocking main loop via PROXY_TO_PTHREAD)
       - Expected: Event loop stays responsive during execution, no 10s timeout, exits 0

    **Node.js flags that may be needed for WASM threading:**
    - `--experimental-wasm-threads` (if Node.js version requires it)
    - `--experimental-wasm-simd` (if Node.js version requires it)
    - Node 18+ should have these enabled by default

    **For each test failure:**
    1. Read the specific error output
    2. Determine if the issue is in the test script (path assumptions, API mismatch) or in the WASM binary (missing exports, link errors)
    3. Fix the test script if it has incorrect assumptions about the binary's exports or paths
    4. If the binary itself has issues, document what needs to change in the build (this would require re-running Plan 04)

    **Blend file loading test (validates gap #2 from VERIFICATION.md):**
    After test-wasm-load.js passes, create a minimal test that also validates blend file loading:
    1. Create a tiny test .blend file or use Blender's factory default startup file if available in the Blender Mirror source
    2. Copy it to a location accessible by the WASM binary (via Node.js FS)
    3. Call wasm_load_blend() and wasm_query_scene() to verify the data pipeline works end-to-end

    If no .blend file is easily available, document this as needing a manual test with a real .blend file.
  </action>
  <verify>
    <automated>node scripts/test-wasm-load.js && node scripts/test-threading.js && node scripts/test-memory.js && node scripts/test-main-loop.js && echo "ALL TESTS PASS" || echo "SOME TESTS FAILED"</automated>
  </verify>
  <acceptance_criteria>
    - All 4 test scripts exit with code 0 and print "PASS:" messages
    - wasm_init() returns 0 (successful initialization)
    - wasm_memory_usage() returns a positive number of bytes
    - SharedArrayBuffer is available (confirms COOP/COEP are effective in Node.js context)
    - OR test failures are documented with root cause analysis and remediation steps
  </acceptance_criteria>
  <done>All four Node.js validation scripts pass, proving BUILD-01 through BUILD-08 are satisfied at runtime. The WASM binary loads, initializes, reports memory, and threading is functional.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Browser validation of WASM binary and dev server</name>
  <files>web/index.html, web/serve.py, build-wasm/blender.wasm</files>
  <action>
    Human verifies the full Phase 1 stack end-to-end in a browser environment.

    What was built: Ran all 4 Node.js WASM validation test scripts against the compiled blender.wasm binary. Fixed any test script issues. Attempted blend file loading validation.

    How to verify:
    1. Run the test suite yourself to confirm results:
       ```
       node scripts/test-wasm-load.js
       node scripts/test-threading.js
       node scripts/test-memory.js
       node scripts/test-main-loop.js
       ```
       Expected: All print "PASS:" and exit 0

    2. Start the dev server and test in browser:
       ```
       python3 web/serve.py 8080 .
       ```
       Then open http://localhost:8080/web/index.html in Chrome.
       Expected: All feature checks show green (COI, SharedArrayBuffer, SIMD, WASM load)

    3. Check COOP/COEP headers:
       ```
       curl -sI http://localhost:8080/ | grep -i cross-origin
       ```
       Expected: Cross-Origin-Opener-Policy: same-origin AND Cross-Origin-Embedder-Policy: require-corp

    4. Verify binary sizes:
       ```
       ls -lh build-wasm/blender.wasm build-wasm/blender.wasm.br
       ```
       Expected: .wasm exists (size reasonable), .wasm.br exists (under 30MB)

    5. (Optional) Load a .blend file in the browser console:
       - Open browser devtools on the test page
       - Upload a .blend file to MEMFS via the test page interface
       - Call Module.ccall('wasm_load_blend', 'number', ['string'], ['/data/test.blend'])
       - Call Module.ccall('wasm_query_scene', 'string', [], [])
       Expected: Returns JSON with scene name and object list
  </action>
  <verify>
    <automated>curl -sI http://localhost:8080/ 2>/dev/null | grep -q "Cross-Origin-Opener-Policy" && echo "PASS: COOP header present" || echo "SKIP: Dev server not running (manual check needed)"</automated>
  </verify>
  <done>Human confirms: all Node.js tests pass, browser test page works with COOP/COEP, WASM loads in browser, and binary sizes are within target. Type "approved" or describe issues.</done>
</task>

</tasks>

<verification>
- All 4 Node.js test scripts pass (exit code 0)
- wasm_init() returns 0 in both Node.js and browser contexts
- SharedArrayBuffer is available (pthreads operational)
- Memory allocation reports accurate usage within 4GB ceiling
- Dev server delivers WASM with COOP/COEP headers
- Browser test page shows all feature checks passing
- Compressed binary size confirmed under 30MB
</verification>

<success_criteria>
All 5 ROADMAP success criteria for Phase 1 are provably satisfied:
1. WASM binary loads in browser with COOP/COEP headers -- no errors
2. .blend file loading and scene querying works via DNA/RNA API
3. Web Workers spawn with SharedArrayBuffer (pthreads operational)
4. Memory stays within 4GB ceiling with accurate MEM_guarded_alloc reporting
5. Compressed binary under 30MB with lazy loading configured
</success_criteria>

<output>
After completion, create `.planning/phases/01-headless-wasm-build/01-05-SUMMARY.md`
</output>

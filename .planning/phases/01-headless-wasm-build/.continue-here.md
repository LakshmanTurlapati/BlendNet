---
phase: 01-headless-wasm-build
task: 2
total_tasks: 3
status: paused
last_updated: 2026-04-02T18:54:00.608Z
---

<current_state>
Phase 01 / Plan 07 / Task 2 is paused mid-gap-closure. Task 1 is done. Task 2 has code changes in the WASM wrapper and build system, but the first full rebuild with the latest linker fix was intentionally interrupted during compile around [360/2355]. All Docker containers are now stopped. `build-wasm/blender.wasm.br` is currently `0` bytes because the interrupted run reached the Brotli step and was cut off.
</current_state>

<completed_work>

- Task 1: Add a reproducible real-.blend debug harness and symbolized failure mode - Done. `scripts/test-blend-query.js` exists, uses the official `Blender-282.blend` sample, and the build path now supports an env-gated debug/symbolized mode without changing the default release path.
- Task 2: Patch the loader/runtime path so a real sample loads and queries successfully - In progress. `source/wasm_headless_main.cc` was updated and the MEM_new compile error was fixed by adding `BKE_wm_runtime.hh`.
- Task 2: Patch the loader/runtime path so a real sample loads and queries successfully - In progress. The reproducible final-link failure was traced to Emscripten's default Freetype port archive pulling in `emscripten_longjmp` under Wasm exceptions. `scripts/build-wasm-deps.sh` now builds `freetype-legacysjlj`, and `scripts/patch-ninja-host-tools.sh` rewrites `build.ninja` to link `libfreetype-legacysjlj.a`. The active build logs confirmed both hooks fired before pause.
</completed_work>

<remaining_work>

- Finish Task 2 by rerunning `docker compose run --rm blender-wasm-build bash scripts/build-wasm-full.sh` and confirming the final link no longer fails on `emscripten_longjmp`.
- If the rebuild succeeds, run `BLEND_SAMPLE_PATH=/tmp/Blender-282.blend node scripts/test-blend-query.js` and verify the real sample no longer traps and `wasm_query_scene()` returns valid JSON.
- Task 3: Rerun `node scripts/test-wasm-load.js`, `node scripts/test-threading.js`, `node scripts/test-memory.js`, and `node scripts/test-main-loop.js`; then confirm artifact sizes and regenerate a valid `build-wasm/blender.wasm.br`.
- Replace placeholder content in `01-05-SUMMARY.md` and `01-06-SUMMARY.md` after verification closes the remaining gap.
</remaining_work>

<decisions_made>

- Decided to use `scripts/test-blend-query.js` plus the official `Blender-282.blend` file as the canonical Phase 01 gap harness because it reproduces the missing truth end to end without manual browser steps.
- Kept debug and symbolization flags behind an env-controlled build mode so the release build stays small unless debugging is explicitly requested.
- Switched the Freetype port handling to `libfreetype-legacysjlj.a` because the default Emscripten archive path is incompatible with the current Wasm-exception build mode.
- Avoided destructive Docker cleanup such as volume pruning because the user asked to free space, but resume should remain straightforward.
</decisions_made>

<blockers>
- Technical blocker: The first rebuild with the `freetype-legacysjlj` fix was interrupted before final link, so the linker result is still unknown.
- Technical blocker: The real `.blend` load path is still unresolved until `BLEND_SAMPLE_PATH=/tmp/Blender-282.blend node scripts/test-blend-query.js` completes without trapping.
- Artifact blocker: `build-wasm/blender.wasm.br` is currently invalid (`0` bytes) because the Brotli step was interrupted.
- Process blocker: `01-05-SUMMARY.md` and `01-06-SUMMARY.md` still contain placeholder text and should be finalized after validation.
</blockers>

<context>
No background processes are left running. `docker ps` is empty. The worktree is dirty and includes both current pause-state files and earlier phase/build edits, so resume carefully and do not revert unrelated changes. The most important mental checkpoint is that the work is no longer blocked on missing assets or unclear repro steps; it is narrowed to proving whether the Freetype legacy-SjLj linker fix gets the rebuild through link, then validating the real `.blend` runtime path on that rebuilt artifact.
</context>

<next_action>
Start with: `docker compose run --rm blender-wasm-build bash scripts/build-wasm-full.sh`

If that links cleanly, immediately run:
`BLEND_SAMPLE_PATH=/tmp/Blender-282.blend node scripts/test-blend-query.js`

Then rerun:
`node scripts/test-wasm-load.js`
`node scripts/test-threading.js`
`node scripts/test-memory.js`
`node scripts/test-main-loop.js`
</next_action>

---
phase: 01-headless-wasm-build
plan: 07
type: execute
wave: 3
depends_on: ["01-05", "01-06"]
files_modified:
  - source/wasm_headless_main.cc
  - cmake/emscripten_overrides.cmake
  - scripts/build-wasm-full.sh
  - scripts/test-blend-query.js
  - patches/*.wasm32.patch
autonomous: false
gap_closure: true
requirements: [BUILD-01, BUILD-02, BUILD-03, BUILD-04, BUILD-05, BUILD-06, BUILD-07, BUILD-08]

must_haves:
  truths:
    - "A symbolized or assertions-enabled reproduction identifies the subsystem behind the wasm_load_blend() indirect-call trap"
    - "wasm_load_blend('/data/Blender-282.blend') returns a non-negative object count instead of trapping"
    - "wasm_query_scene() returns valid JSON for the loaded sample scene"
    - "Release artifacts still pass the Node.js and browser runtime checks after the loader fix"
  artifacts:
    - path: "scripts/test-blend-query.js"
      provides: "Dedicated end-to-end harness for MEMFS .blend loading and scene-query validation"
    - path: "build-wasm/blender.wasm"
      provides: "Release WASM binary with a working real-.blend loader path"
    - path: "build-wasm/blender.wasm.br"
      provides: "Compressed release artifact still under the Phase 1 size target"
  key_links:
    - from: "scripts/test-blend-query.js"
      to: "build-wasm/blender.js"
      via: "Loads the Emscripten glue and exercises wasm_load_blend()/wasm_query_scene() against a real sample"
      pattern: "wasm_load_blend|wasm_query_scene"
    - from: "source/wasm_headless_main.cc"
      to: "Blender Mirror/source/blender/blenloader"
      via: "Calls BLO_read_from_file() and post-load scene traversal"
      pattern: "BLO_read_from_file"
    - from: "scripts/build-wasm-full.sh"
      to: "cmake/emscripten_overrides.cmake"
      via: "Optional debug/assertions build mode for symbolized trap reproduction"
      pattern: "ASSERTIONS|SAFE_HEAP|profil"
---

<objective>
Close the remaining Phase 01 blocker by turning the current real-.blend runtime trap into a symbolized/debuggable failure, fixing the underlying loader/runtime defect, and proving that a real `.blend` file can be loaded and queried end-to-end.

Purpose: Phase 01 already has a clean scripted Docker build, passing Node/browser runtime validation, and a proven official sample input. The only remaining failure is that `wasm_load_blend()` traps with `RuntimeError: null function or function signature mismatch` when loading `https://download.blender.org/demo/Blender-282.blend`. This plan converts that failure into a named root cause, patches it in the repo-owned WASM integration layer (or a tracked source patch if the defect is inside Blender code), and re-runs the full validation chain.

Output: one dedicated real-blend validation harness, one working loader/runtime fix, and revalidated release artifacts that satisfy the original scene-query truth for Phase 01
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-headless-wasm-build/01-VERIFICATION.md
@.planning/phases/01-headless-wasm-build/01-UAT.md
@.planning/phases/01-headless-wasm-build/01-05-SUMMARY.md
@.planning/phases/01-headless-wasm-build/01-06-SUMMARY.md

Key implementation files:
@source/wasm_headless_main.cc
@cmake/emscripten_overrides.cmake
@scripts/build-wasm-full.sh
@scripts/test-wasm-load.js
@scripts/test-threading.js
@scripts/test-memory.js
@scripts/test-main-loop.js

<interfaces>
Current verified state:
- `build-wasm/blender.wasm` exists and loads in Node/browser
- `build-wasm/blender.wasm.br` exists and is under 30 MB
- `wasm_init()` returns `0`
- `wasm_memory_usage()` returns `1620` bytes after init
- `scripts/test-wasm-load.js`, `scripts/test-threading.js`, `scripts/test-memory.js`, and `scripts/test-main-loop.js` all pass

Current blocker:
- Official sample: `https://download.blender.org/demo/Blender-282.blend`
- Repro: copy file into MEMFS, call `wasm_load_blend('/data/Blender-282.blend')`
- Failure: `RuntimeError: null function or function signature mismatch`
- Current likely fault boundary: `BLO_read_from_file()` path or a post-load callback reached by `wasm_load_blend()`

Allowed source-fix mechanism:
- Repo-owned glue/runtime files may be edited directly
- If the root cause lives in Blender source, add or update a tracked patch under `patches/*.wasm32.patch`
- `scripts/build-wasm-full.sh` already applies tracked `*.wasm32.patch` files before configuring/building
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a reproducible real-.blend debug harness and symbolized failure mode</name>
  <files>scripts/test-blend-query.js, scripts/build-wasm-full.sh, cmake/emscripten_overrides.cmake</files>
  <read_first>
    - scripts/test-wasm-load.js (existing module bootstrap pattern)
    - scripts/test-main-loop.js (existing runtime initialization pattern)
    - scripts/build-wasm-full.sh (current end-to-end Docker build orchestration)
    - cmake/emscripten_overrides.cmake (current release flags and exported functions)
    - .planning/phases/01-headless-wasm-build/01-UAT.md (exact diagnosed gap)
  </read_first>
  <action>
    Create a dedicated harness for the real `.blend` truth and make the current trap debuggable.

    1. Add `scripts/test-blend-query.js`:
       - Bootstrap `build-wasm/blender.js` exactly like the other Node tests
       - Accept `BLEND_SAMPLE_PATH` and default to `/tmp/Blender-282.blend`
       - If the sample is missing locally, download `https://download.blender.org/demo/Blender-282.blend` to `/tmp/Blender-282.blend`
       - Write the file into MEMFS at `/data/Blender-282.blend`
       - Call `wasm_init()`, `wasm_load_blend()`, and `wasm_query_scene()`
       - Fail loudly on traps, include the JS/WASM stack, and parse the JSON result when successful

    2. Extend the build path with a debug/symbolized mode that does NOT permanently bloat the release artifact:
       - Add an env-controlled path such as `WASM_DEBUG_TRAP=1` or `WASM_BUILD_MODE=debug`
       - In that mode, append assertion/symbolization flags such as `-sASSERTIONS`, `-sSAFE_HEAP`, `-sSTACK_OVERFLOW_CHECK`, `-sDEMANGLE_SUPPORT`, and profiling/source-map style debug info as supported by the current Emscripten toolchain
       - Keep the default release path unchanged unless a flag is explicitly set

    3. Use the new harness plus debug build to reproduce the failure with a stack or assertion message that is good enough to name the failing subsystem or callback family.

    4. Record the concrete failing boundary in the plan summary:
       - example outcomes: blend-file versioning callback, readfile postprocess callback, allocator misuse, unsupported indirect-call target, or wrapper-side misuse of the returned data

    The goal of this task is not "more logs" in the abstract. The goal is one reproducible command path that turns the current minified trap into a named implementation problem.
  </action>
  <verify>
    <automated>BLEND_SAMPLE_PATH=/tmp/Blender-282.blend node scripts/test-blend-query.js</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/test-blend-query.js` exists and reproduces the real sample path without manual console steps
    - A debug/symbolized build mode exists without changing the default release build path
    - The failing subsystem or callback boundary is identified from the resulting trace/assertion output
  </acceptance_criteria>
  <done>A dedicated real-.blend harness exists, and the previously minified trap is reduced to a named failing subsystem with enough evidence to patch it.</done>
</task>

<task type="auto">
  <name>Task 2: Patch the loader/runtime path so a real sample loads and queries successfully</name>
  <files>source/wasm_headless_main.cc, patches/*.wasm32.patch, cmake/emscripten_overrides.cmake</files>
  <read_first>
    - source/wasm_headless_main.cc (current init/load/query flow)
    - .planning/phases/01-headless-wasm-build/01-VERIFICATION.md (remaining gap statement)
    - Any source patch identified by Task 1 as necessary inside `patches/*.wasm32.patch`
  </read_first>
  <action>
    Fix the actual failure revealed by Task 1.

    Use this decision order:

    1. If the failure is caused by repo-owned WASM wrapper logic:
       - Patch `source/wasm_headless_main.cc`
       - Correct initialization order, file-load flags, ownership transfer, error handling, or post-load scene traversal as required

    2. If the failure is inside Blender source that cannot be fixed from the wrapper:
       - Create or update a tracked `patches/*.wasm32.patch`
       - Keep the patch minimal and targeted to the exact failing subsystem identified in Task 1
       - Ensure `scripts/build-wasm-full.sh` still applies it cleanly and idempotently

    3. Rebuild the release artifact after the fix and re-run `scripts/test-blend-query.js`

    4. Validate end-to-end success:
       - `wasm_load_blend('/data/Blender-282.blend')` returns a non-negative object count
       - `wasm_query_scene()` returns parseable JSON
       - JSON includes a scene record and at least one object entry, or explicitly explains an empty valid scene if that is what the sample contains

    Avoid "fixes" that merely suppress the trap while leaving the phase truth false. The final state must actually load the real sample and surface queryable scene data.
  </action>
  <verify>
    <automated>BLEND_SAMPLE_PATH=/tmp/Blender-282.blend node scripts/test-blend-query.js</automated>
  </verify>
  <acceptance_criteria>
    - The official sample no longer traps inside `wasm_load_blend()`
    - `wasm_load_blend()` returns a non-negative object count
    - `wasm_query_scene()` returns valid JSON for the loaded data
    - Any source-side fix lives in tracked repo patches rather than ad-hoc edits inside the mounted Blender mirror
  </acceptance_criteria>
  <done>The real sample `.blend` loads through MEMFS, the loader no longer traps, and the exported query API returns usable scene JSON.</done>
</task>

<task type="auto">
  <name>Task 3: Revalidate the Phase 01 release artifact and guard against regressions</name>
  <files>scripts/test-wasm-load.js, scripts/test-threading.js, scripts/test-memory.js, scripts/test-main-loop.js, scripts/test-blend-query.js, build-wasm/blender.wasm, build-wasm/blender.wasm.br</files>
  <read_first>
    - scripts/test-wasm-load.js
    - scripts/test-threading.js
    - scripts/test-memory.js
    - scripts/test-main-loop.js
    - scripts/test-blend-query.js
    - .planning/phases/01-headless-wasm-build/01-05-SUMMARY.md
    - .planning/phases/01-headless-wasm-build/01-06-SUMMARY.md
  </read_first>
  <action>
    Re-run the full validation chain against the fixed release build.

    1. Rebuild the release artifact through the scripted Docker path:
       - `docker compose run --rm blender-wasm-build bash scripts/build-wasm-full.sh`

    2. Re-run the existing Node validation suite:
       - `node scripts/test-wasm-load.js`
       - `node scripts/test-threading.js`
       - `node scripts/test-memory.js`
       - `node scripts/test-main-loop.js`

    3. Re-run the new real-sample harness:
       - `BLEND_SAMPLE_PATH=/tmp/Blender-282.blend node scripts/test-blend-query.js`

    4. Re-run the browser smoke path if the release glue or exported interface changed:
       - Confirm `web/index.html` still reaches runtime init, `wasm_init()`, and `wasm_memory_usage()`
       - If a previous browser automation script exists locally, reuse it; otherwise document the exact manual/browser command used

    5. Confirm artifact sizes again:
       - `build-wasm/blender.wasm`
       - `build-wasm/blender.wasm.br`
       - Keep the compressed artifact under 30 MB

    This task closes the loop: the fix must preserve the already-working browser/runtime/threading/memory truths while adding the missing real-blend truth.
  </action>
  <verify>
    <automated>node scripts/test-wasm-load.js && node scripts/test-threading.js && node scripts/test-memory.js && node scripts/test-main-loop.js && BLEND_SAMPLE_PATH=/tmp/Blender-282.blend node scripts/test-blend-query.js</automated>
  </verify>
  <acceptance_criteria>
    - All existing Node validation scripts still pass
    - The new real-sample harness passes
    - Browser runtime initialization still works if the exported interface changed
    - `build-wasm/blender.wasm.br` remains under the 30 MB compressed target
  </acceptance_criteria>
  <done>The release build is revalidated end-to-end, and Phase 01's final missing truth is now proven without regressing the previously passing runtime checks.</done>
</task>

</tasks>

<verification>
- Debug build mode reproduces the failure with enough symbol/assertion detail to identify the failing subsystem
- `scripts/test-blend-query.js` passes against the official sample
- `node scripts/test-wasm-load.js` passes
- `node scripts/test-threading.js` passes
- `node scripts/test-memory.js` passes
- `node scripts/test-main-loop.js` passes
- `build-wasm/blender.wasm.br` remains under 30 MB
</verification>

<success_criteria>
The last missing Phase 01 truth is now demonstrably satisfied:
1. A real `.blend` file loads from MEMFS without trapping
2. The exported scene-query API returns valid JSON for that loaded file
3. The fix preserves the already-proven browser/runtime/threading/memory/size requirements
</success_criteria>
